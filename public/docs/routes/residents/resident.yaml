get:
  description: Retorna um morador
  tags:
    - Moradores
  parameters:
    - $ref: ../../common/parameters/path.yaml#parameters/id
  responses:
    "200":
      description: Morador encontrado
      content:
        application/json:
          schema:
            $ref: components/schemas/standard.yaml#Resident
delete:
  description: Deletar morador
  tags:
    - Moradores
  parameters:
    - $ref: ../../common/parameters/path.yaml#parameters/id
  responses:
    "200":
      description: Despesa deletada com sucesso
